name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:

  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dev dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements_CI.txt
          pip install -r requirements-dev.txt

      - name: Run linter (ruff)
        run: ./scripts/lint.sh

      - name: Run all tests (unit + integration) - without local data
        env:
          PYTHONPATH: src
        run: ./scripts/test_all.sh

  # test:
  #   runs-on: ubuntu-latest
  #   needs: lint   # lint 过了才跑测试

  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4

  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.12"

  #     - name: Install dev dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -r requirements-dev.txt

  #     - name: Run unit tests
  #       env:
  #         PYTHONPATH: src
  #       run: ./scripts/test_unit.sh

  #     以后你想加 integration，可以改成：
      

  # 可选：类型检查（等你愿意严格一点时再打开）
  # typecheck:
  #   runs-on: ubuntu-latest
  #   needs: test
  
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  
  #     - name: Set up Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.12"
  
  #     - name: Install dev dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -r requirements-dev.txt
  
  #     - name: Run mypy
  #       env:
  #         PYTHONPATH: src
  #       run: ./scripts/typecheck.sh
